import CartPageClient from './CartPageClient';

export const metadata = {
  title: 'Panier — JWL Marketing',
  description: 'Vérifiez votre panier avant de finaliser l’achat de vos packs marketing digital et SEO.',
};

export default function CartPage() {
  return <CartPageClient />;
}
